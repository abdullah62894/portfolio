<section class="hero-section" id="hero">
    <div class="hero-bg-grid"></div>
    <div class="hero-bg-gradient"></div>

    <div class="container">
        <div class="hero-content">
            <div class="hero-badge">
                <span class="hero-badge-dot"></span>
                Available for enterprise opportunities
            </div>

            <h1 class="hero-title">
                <span class="hero-title-line">Enterprise .NET</span>
                <span class="hero-title-accent">Modernization Engineer</span>
            </h1>

            <p class="hero-subtitle">
                Helping organizations migrate legacy .NET Framework applications into scalable, secure, modern .NET solutions.
            </p>

            <div class="hero-actions">
                <RadzenButton ButtonType="ButtonType.Button" ButtonStyle="ButtonStyle.Primary" Size="ButtonSize.Large" class="hero-btn-primary" Click="@ScrollToProjects">
                    View Projects
                </RadzenButton>
                <RadzenButton ButtonType="ButtonType.Button" ButtonStyle="ButtonStyle.Secondary" Size="ButtonSize.Large" Click="@DownloadResume">
                    Download Resume
                </RadzenButton>
                <RadzenButton ButtonType="ButtonType.Button" ButtonStyle="ButtonStyle.Base" Size="ButtonSize.Large" class="hero-btn-outline" Click="@ScrollToContact">
                    Contact Me
                </RadzenButton>
            </div>

            <div class="hero-stats">
                <div class="hero-stat">
                    <div class="hero-stat-value hero-stat-accent">4+</div>
                    <div class="hero-stat-label">Years Experience</div>
                </div>
                <div class="hero-stat">
                    <div class="hero-stat-value">3</div>
                    <div class="hero-stat-label">Portfolio Repos</div>
                </div>
                <div class="hero-stat">
                    <div class="hero-stat-value hero-stat-accent">.NET 9</div>
                    <div class="hero-stat-label">Modernization Stack</div>
                </div>
            </div>
        </div>
    </div>
</section>

@code {
    [Inject] private IJSRuntime JSRuntime { get; set; } = null!;

    private async Task ScrollToProjects()
    {
        await JSRuntime.InvokeVoidAsync("scrollToSection", "projects");
    }

    private async Task ScrollToContact()
    {
        await JSRuntime.InvokeVoidAsync("scrollToSection", "contact");
    }

    private async Task DownloadResume()
    {
        await JSRuntime.InvokeVoidAsync("downloadResume");
    }
}
